# 当前基线与后续任务

日期：2026-06-05

## 当前工作基线

当前唯一工作基线为：

```text
01_模型/CEGR_Vehicle_10kW_GZS60_v03_stage1_pressurefix.slx
```

当前三个工作入口为：

```text
02_脚本/init_vehicle_10kw_gzs60_v3.m
02_脚本/calibrate_vehicle_10kw_gzs60_v3_pressurefix_stage1.m
02_脚本/calibrate_vehicle_10kw_gzs60_v3_thermal_stageA.m
02_脚本/calibrate_vehicle_10kw_gzs60_v3_humidity_stageB.m
```

`init_vehicle_10kw_gzs60_v3.m` 默认指向 `stage1_pressurefix` 模型，并优先读取：

```text
00_输入参数/标定参数/pressurefix_stage1_boundary_params.csv
00_输入参数/标定参数/thermal_stageA_params.csv
00_输入参数/标定参数/thermal_stageA_cooling_flow_curve.csv
00_输入参数/标定参数/humidity_stageB_params.csv
```

若对应文件不存在，才回退读取一周目 `stage1_boundary_calibrated_params.csv` 或
`stage2_thermal_humidity_calibrated_params.csv`，用于首次运行或回看。

## 压力修正口径

本轮二周目不复刻一周目的完整三阶段拟合流程。当前目标是修正电堆模块压力传递语义，并只重拟合压力/流量链参数。

已冻结的口径：

- `V_ca_m3 = 2.0e-4 m3`，`V_an_m3 = 1.5e-4 m3`，按电堆阴/阳极流道气体体积处理，不再用稳态误差反向调大。
- `p_stack_internal_kPa` 只作为电堆内部库存压力诊断量。
- 阴极出口节点压力 `caOut(6)` 为阴极出口/背压边界 `pcBack`，不再输出堆内库存压力。
- 阳极出口节点压力 `anOut(6)` 为阳极出口/背压边界 `panBack`，不再输出堆内库存压力。
- 出口流量仍由堆内压力和出口边界压力差驱动。

## 当前验证结果

最新压力修正输出为：

```text
04_验证结果/pressurefix_stage1_no_egr_diagnostic.csv
04_验证结果/pressurefix_stage1_candidate_no_egr_diagnostic.csv
04_验证结果/pressurefix_stage1_summary.md
```

13 点无 EGR 压力诊断结果：

- 压力顺序：13/13
- 入口压力 RMSE：0.010847 kPa
- 出口边界最大误差：0 kPa
- `min(p_ca_in_sim - p_stack_internal) = 3.4265 kPa`
- `min(p_stack_internal - p_ca_out_boundary) = 4.0733 kPa`
- `min(lambda_O2_actual) = 1.2999`
- `V_cell RMSE = 0.005644 V/cell`
- `RH_ca_in RMSE = 0.188`
- `T_stack RMSE = 18.692 C`
- 当前稳态判据通过：5/13

稳态判据未通过的 8 个点主要体现热动态仍未收敛到原脚本的严格窗口；这不否定本轮压力语义修正，但后续解释湿度、温度和热量回归时必须单独处理。

## 当前热侧 Stage A 结果

本轮已完成热侧阶段 A，并已将其接入当前工作基线。当前热侧入口为：

```text
02_脚本/calibrate_vehicle_10kw_gzs60_v3_thermal_stageA.m
```

当前热侧参数文件与冷却曲线文件为：

```text
00_输入参数/标定参数/thermal_stageA_params.csv
00_输入参数/标定参数/thermal_stageA_cooling_flow_curve.csv
```

当前热侧输出为：

```text
04_验证结果/thermal_stageA_diagnostic.csv
04_验证结果/thermal_stageA_candidate_grid.csv
04_验证结果/thermal_stageA_summary.md
```

已冻结的热侧口径：

- 二周目先热后湿，阶段 A 只做热侧，不主动拟合湿度参数。
- `C_stack_J_K` 冻结，当前主要研究稳态工况，不用工况终点误差反推热惯性。
- 冷却增强不再按电流拟合，而按 `coolant_flow_L_min -> h_cool_eff` 建立等效换热曲线。
- 台架冷却带热量按水当量近似反算：
  `rho = 1.0 kg/L`，`cp = 4180 J/kg/K`。
- 阶段 A 以 `T_stack` 为主目标，以 `Q_cool,bench` 为辅助约束。
- 阶段 A 中 `hum_heat_eff` 保持冻结，不提前把湿侧传热混入热侧拟合。

当前热侧阶段 A 结果：

- `T_stack RMSE = 0.570 C`
- `Q_cool RMSE = 635.2 W`
- `Q_cool bias = 25.0 W`
- 稳态点：`13/13`
- 压力顺序：`13/13`
- `RH_ca_in RMSE = 0.133`（仅回归检查）
- `V_cell RMSE = 0.0804 V/cell`（仅回归检查）
- `min(lambda_O2_actual) = 1.788`

当前冷却流量-等效换热曲线支撑点为：

```text
5.5  ->   836.0 W/K
6.0  ->   836.0 W/K
6.5  ->   905.7 W/K
7.5  ->  1045.0 W/K
9.1  ->  1267.9 W/K
10.0 ->  1393.3 W/K
11.1 ->  1546.6 W/K
11.5 ->  1602.3 W/K
```

说明：

- 低负荷点 `bench_j0p10` 存在 `coolant_outlet_temp_C < coolant_inlet_temp_C`，对应台架反算 `Q_cool,bench < 0`；该点保留在温度回归中，但不作为强热平衡真值点。
- 当前 `current` 模式已优先接入 `thermal_stageA_params.csv` 和 `thermal_stageA_cooling_flow_curve.csv`，即默认工作基线已包含热侧阶段 A 结果。

## 文件保留边界

主工作目录已按当前链路收口：

- `01_模型` 只保留当前可信模型 `CEGR_Vehicle_10kW_GZS60_v03_stage1_pressurefix.slx`。
- `02_脚本` 只保留初始化入口和二周目压力、热侧 Stage A、湿侧 Stage B 入口。
- `04_验证结果` 只保留当前基线需要的 pressurefix、thermal Stage A、humidity Stage B 诊断输出。

一周目过程模型、旧脚本和旧验证结果不再放在主模型/脚本/结果目录中，避免与当前基线混淆。历史证据以 Git 或 `05_历史归档` 为准。

## 后续任务

### 1. 湿侧 Stage B 当前结果

本轮已完成湿侧 Stage B 第一轮，并已将其接入当前工作基线。当前湿侧入口为：

```text
02_脚本/calibrate_vehicle_10kw_gzs60_v3_humidity_stageB.m
```

当前湿侧参数文件与输出为：

```text
00_输入参数/标定参数/humidity_stageB_params.csv
04_验证结果/humidity_stageB_no_egr_diagnostic.csv
04_验证结果/humidity_stageB_hum_prior_replay.csv
04_验证结果/humidity_stageB_candidate_trace.csv
04_验证结果/humidity_stageB_summary.md
```

已冻结的 Stage B 第一轮口径：

- 只用 13 个无 EGR 稳态点做拟合；
- 以当前热侧 Stage A 为冻结基线；
- 主指标为 `pH2O_caIn_kPa`，第二指标为 `xH2O_caIn`，`RH_ca_in` 只做辅助校核；
- 四口加湿器数据只作次级先验，不与堆入口湿态平权混合；
- 第一轮只放开 `hum_NTU_ref`、`hum_flow_exp`、`hum_mem_D_eff_m2_s`；
- 不主动改动压力链参数、`C_stack_J_K` 或堆内膜水参数。

当前 Stage B 第一轮结果：

- `pH2O_caIn RMSE = 4.500 kPa`，较基线 `7.894 kPa` 明显下降；
- `xH2O_caIn RMSE = 0.02171`，较基线 `0.03640` 下降；
- `RH_ca_in RMSE = 0.166`，较热侧 Stage A 基线 `0.133` 略有变差；
- 四口先验 `dry/wet omega RMSE = 71.92 / 56.52 g/kg`，与基线基本同量级；
- `T_stack RMSE = 0.585 C`，相对 Stage A 的 `0.570 C` 仅轻微变差；
- `V_cell RMSE = 0.0735 V/cell`，较 Stage A 的 `0.0804 V/cell` 略有改善；
- 压力顺序继续 `13/13` 通过。

当前接受的 Stage B 参数为：

```text
hum_NTU_ref        = 0.215343
hum_flow_exp       = 0.37998
hum_mem_D_eff_m2_s = 1.25e-09
```

结论：

- Stage B 第一轮已经证明：在不破坏压力链和热侧基线的前提下，堆入口水蒸气分压和含湿量可以明显改善；
- 但 `RH_ca_in` 没有同步改善，且四口先验并未显著变好，因此当前结果更适合定义为“第一轮可接受修正”，还不是湿侧最终收敛版本。

### 2. 后续优先任务

湿侧下一步不回到一周目整套流程，而是针对 Stage B 第一轮剩余偏差做选择性推进：

1. 先复核 `RH_ca_in` 与 `pH2O_caIn` 不同步改善的原因，确认是否主要来自温度换算口径；
2. 再决定是否进入 Stage B 第二轮，放开 `hum_beta_dry_m_s / hum_beta_wet_m_s`；
3. 若四口先验需要更强约束，再单独提高其权重，不直接破坏堆入口主目标。

### 3. 电压回归复核

电压参数当前仍沿用一周目结果。后续只在以下条件同时满足后再决定是否复核：

1. 压力链已固定；
2. 热侧阶段 A 已接受；
3. 湿侧阶段 B 已达到当前可接受边界；
4. `V_cell` 仍存在明确系统偏差。
