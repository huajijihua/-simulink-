# v3 迁移前检查与下一步计划

更新时间：2026-06-03

## 1. 当前主线

当前继续推进的主线已经切换到：

`01_自吸方案/01_车载系统_10kW_GZS60_v3`

旧目录 `01_自吸方案/01_车载系统_10kW_GZS60` 和 `01_自吸方案/01_车载系统_10kW_GZS60_v2` 已完成可复用资料提炼，并已从当前项目文件夹删除；后续建模研究只保留 `v3` 主线。

当前权威模型为：

`01_模型/CEGR_Vehicle_10kW_GZS60_v03.slx`

当前权威入口脚本位于：

`02_脚本`

## 2. 当前模型状态

v3 已完成从旧模型向简化、清晰接口模型的第一轮重构。

顶层阴极气路为：

```text
EnvironmentFreshAir
-> EGRMixer
-> Compressor
-> Intercooler
-> HumidifierDry
-> PEMFCStack
-> BackPressure
-> HumidifierWet
-> Separator
-> EGRSplitter
-> EGR_return_delay / vent
```

模块间主气体接口已经收敛为 7 维节点：

```text
[m_O2_kg_s
 m_N2_kg_s
 m_H2O_v_kg_s
 m_H2O_l_kg_s
 T_C
 p_kPa
 liquid_present]
```

诊断量不进入模块主链路，由验证脚本从节点和摘要向量派生。

## 3. 电堆极化标定状态

电压模型已按当前要求切换为新书籍公式结构：

```text
V_cell = E - V_act - V_ohm - V_conc
V_act = -[xi1 + xi2*T + xi3*T*ln(C_O2) + xi4*T*ln(I)]
V_ohm = I*(rho_m(i,T,lambda_m)*L_m/A_cell + R_c)
V_conc = m*exp(n*I)
```

旧极化拟合链路已废弃并删除：

- `stack_nernst_voltage_params.csv`
- `stack_nernst_voltage_13point_fit.csv`
- `stack_no_egr_physics_params.csv`
- `run_vehicle_10kw_gzs60_v3_polarization_check.m`
- `fit_vehicle_10kw_gzs60_v3_polarization.m`

说明：

- 当前不能再引用旧 13 点极化拟合误差作为模型验收结论。
- 后续需要基于新书公式重新建立无 EGR 极化标定。
- 在重新标定前，当前电压输出只能作为模型结构运行结果，不作为定量极化预测结论。

## 4. 迁移前复跑结果

本轮已复跑：

1. `build_vehicle_10kw_gzs60_v3`
2. `check_vehicle_10kw_gzs60_v3`
3. `run_vehicle_10kw_gzs60_v3_validation`

关键脚本 MATLAB Code Analyzer 检查均无问题。

系统代表工况验证结果：

| 工况 | EGR | I (A) | V_cell (V) | pO2_in (kPa) | xO2_in | pO2_ca (kPa) | max残差 (kg/s) |
|---|---:|---:|---:|---:|---:|---:|---:|
| cc_no_egr | 0.00 | 120.0 | 0.7486 | 27.967 | 0.1890 | 11.821 | 6.78e-20 |
| cc_egr_0p1 | 0.10 | 120.0 | 0.7479 | 26.620 | 0.1799 | 10.646 | 2.17e-19 |

趋势检查：

- EGR 后入堆氧分压下降：通过。
- EGR 后入堆氧摩尔分数下降：通过。
- EGR 后电堆阴极氧分压下降：通过。
- 恒电流 EGR 后电压不升高：通过。
- 物种级残差小于 1e-8 kg/s：通过。
- 能量残差字段闭合：通过。

## 5. 当前限制

当前模型仍属于结构、守恒和趋势验证版本，不应作为最终系统性能结论。

当前不能声明：

- GZS60 加湿器已经完成直接实测定量标定。
- EGR 阀、回流管路和混合器压降已经完成物理驱动力建模。
- 空压机功耗、净功率收益、系统效率和整车收益已经可信。
- 贫氧或 EGR 工况下电压修正已经完成实验验证。
- 寿命衰减或耐久性收益已有定量结论。

## 6. 迁移时建议保留

当前建议完整保留以下目录：

- `00_支撑材料`
- `01_自吸方案/01_车载系统_10kW_GZS60_v3`
其中 `v3` 是当前继续推进入口。初版和 `v2` 可复用内容已提炼到：

- `00_输入参数/旧版提炼`
- `03_说明/v3_旧版可复用材料提炼.md`

因此在当前仓库状态下，后续应只在 `v3` 基础上建模研究。

## 7. 迁移时可清理

以下内容是 MATLAB / Simulink 生成缓存，可以不作为正式交付内容：

- `slprj`
- 旧版本 `*.slxc`
- `*.autosave`

当前 `CEGR_Vehicle_10kW_GZS60_v03.slxc` 是当前模型缓存，可保留；旧版 `v02` 缓存可清理。如果新电脑首次运行较慢，通常是因为 Simulink 重新生成缓存。

## 8. 新电脑首次运行顺序

在 MATLAB 中切换到：

`01_自吸方案/01_车载系统_10kW_GZS60_v3/02_脚本`

建议依次运行：

```matlab
init_vehicle_10kw_gzs60_v3
build_vehicle_10kw_gzs60_v3
run_vehicle_10kw_gzs60_v3_validation
```

验收标准：

- `v3_validation_summary.md` 中模块级检查全为 PASS。
- 无 EGR / EGR 0.1 两个代表工况可正常生成 `v3_system_summary.csv` 和 `v3_node_table.csv`。

## 9. 下一步推进计划

### 第一阶段：把 v3 作为正式主线固化

目标：

- 根目录迁移说明已切换为 `v3` 主线，后续继续保持同步。
- 统一说明文档中的电压公式，删除或标注旧等效 ASR 拟合口径。
- 明确 `slprj`、`*.slxc` 不纳入正式迁移包。

交付：

- 新版根目录 `项目迁移说明.md`。
- v3 入口 README 或简短运行说明。

### 第二阶段：扩展系统工况

目标：

- 在当前恒电流 120 A 基础上，增加电流扫点。
- 增加 EGR 比例扫点，但先限定在不触发严重氧饥饿的范围。
- 增加固定入堆氧分压口径，评估 EGR 对流量需求和湿度的影响。

交付：

- v3 工况矩阵 CSV。
- EGR 比例趋势图。
- 固定氧分压对比图。

### 第三阶段：补齐设备边界

目标：

- 用 DQ60 空压机 MAP 替代当前简单升压升温边界。
- 将 EGR 阀/回流管路从压力诊断升级为压降和流量驱动模型。
- 对 GZS60 加湿器标定边界做单独审计，区分说明书约束、系列实测外推和待实测参数。

交付：

- 空压机功耗和边界校核表。
- EGR 阀/管路压降模型说明。
- GZS60 加湿器待测参数清单。

### 第四阶段：实验闭环

目标：

- 明确下一轮台架必须测的变量：阴极入口/出口温湿压流、EGR 比例、堆电压、单片电压、HFR、空气/氧浓度边界。
- 用实测 HFR 和不同氧浓度曲线重新拆分欧姆、活化和浓差损失。
- 将贫氧/EGR 下电压修正从趋势假设升级为实验约束。

交付：

- 台架数据接口模板。
- 电压模型再标定报告。
- EGR 工况有效性说明。

